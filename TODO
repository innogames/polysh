o "(yes date | head -n 1000; echo ps) | ./gsh.py --ssh='bash;:' dummy" does not
terminate because of the readline usage in bash.
o restore logging & debugging after hide_password
o :upload => dummy remote_dispatcher for /bin/sh, trigger tar c .. & pity.py replicate
o factorize gsh-... pattern search in remote shell output
